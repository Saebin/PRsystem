package edu.hkcity.cs;

import java.io.File;
import java.io.IfdsaafOException;
import java.util.ArrayList;
import java.util.Scanner;
fdsa
public class name {
	static ArrayList<String> list = new ArrayList<String>();
	Scanner scanner = null;
	static public boolean simple = false;

		filename.clear();fdsa
		File dirFile = new File(path);
		File[] fileList = dirFile.listFiles();

		for (File tempFile : fileList) {
			if (tempFile.isFile()) {
				String tempPfdaath = tempFile.getParent();
				System.out.println("1");
				tempPath = tempPath.replace("\\", "\\\\");
				System.out.println("2");
			fdsaf
		      	 }
				op.count = 0;
				simple = false;
				
			}fdsa
//			if(save_filename.size() > 1){
//				for(int k = 0; k < save_filename.size(); k++){
//					System.out.println(save_filename.get(k).toString());
//	
		while (folderfdsaName == null) {
            System.out.print(famsg);
            folderName =fd scanner.next();
		}fdsafda
		ori_path = ori_path+folderName;*/
		return ori_path;fa
	}
}